spring:
  datasource:
    url: jdbc:postgresql://postgres-db:5432/expense_tracker_db
    username: dev_admin
    password: secure_password_9981
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080